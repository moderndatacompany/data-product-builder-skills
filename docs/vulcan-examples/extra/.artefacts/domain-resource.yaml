version: v1alpha
type: vulcan
name: sample-vulcan-dp
tags:
  - test
spec:
  runAsUser: "rakeshvishvakarmatmdcio"
  compute: desert-compute
  engine: postgres
  repo: #enforced full as it is
    url: https://bitbucket.org/rubik_/vulcan-examples
    syncFlags:
      - '--ref=main'
      - '--submodules=off'
    baseDir: vulcan-examples/postgres-depot
    secret: system:vulcan-git-sync-secret # make it optional
  depots: #enforced at-least 1
    - dataos://vulcanwarehousepostgres?purpose=rw
  workflow: # enforced
    # Dynamic based on the available cron
    type: schedule #enum json schema
    schedule: #optional
      crons:
      - '*/5 * * * *'
      endOn: '2027-01-01T00:00:00-00:00'
      timezone: 'US/Pacific'
      concurrencyPolicy: Forbid

    logLevel: INFO #default
    resource: #optional
      request:
        cpu: "200m"
        memory: "512Mi"
      limit:
        cpu: "1000m"
        memory: "1Gi"

    #NOTE: - refer document for vulcan commands - https://tmdc-io.github.io/vulcan-book/cli-command/cli/?h=commands
    
    plan:
      command:
        - vulcan
      arguments:
        - plan
        - --auto-apply
    run:
      command: # enforced
        - vulcan
      arguments: #Optional
        - run

  api:
    replicas: 1
    logLevel: INFO
    resource:
      request:
        cpu: "200m"
        memory: "512Mi"
      limit:
        cpu: "1000m"
        memory: "1Gi"
