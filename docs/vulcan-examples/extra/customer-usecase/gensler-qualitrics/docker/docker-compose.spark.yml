version: "3.9"

networks:
  vulcan:
    external: true

services:
  spark-master:
    image: spark:3.5.1-scala2.12-java17-python3-ubuntu
    container_name: spark-master
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
    environment:
      SPARK_MASTER_HOST: spark-master
      SPARK_MASTER_PORT: 7077
      SPARK_MASTER_WEBUI_PORT: 8081
    ports:
      - "7077:7077"   # Spark master
      - "8081:8081"   # Spark master UI
    networks:
      - vulcan

  spark-worker-1:
    image: spark:3.5.1-scala2.12-java17-python3-ubuntu
    container_name: spark-worker-1
    depends_on:
      - spark-master
    environment:
      SPARK_WORKER_CORES: 4
      SPARK_WORKER_MEMORY: 4g
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      spark://spark-master:7077
    networks:
      - vulcan

  spark-worker-2:
    image: spark:3.5.1-scala2.12-java17-python3-ubuntu
    container_name: spark-worker-2
    depends_on:
      - spark-master
    environment:
      SPARK_WORKER_CORES: 4
      SPARK_WORKER_MEMORY: 4g
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      spark://spark-master:7077
    networks:
      - vulcan


  # spark-thrift:
  #   image: spark:3.5.1-scala2.12-java17-python3-ubuntu
  #   container_name: spark-thrift
  #   depends_on:
  #     - spark-master
  #   environment:
  #     # Keep the Thrift server in the foreground so the container stays alive
  #     SPARK_NO_DAEMONIZE: "1"
  #   # Start Spark SQL Thrift Server (HiveServer2 / JDBC) and point it at the master
  #   command: >
  #     /opt/spark/sbin/start-thriftserver.sh
  #     --master spark://spark-master:7077
  #     --hiveconf hive.server2.thrift.port=10000
  #     --hiveconf hive.server2.thrift.bind.host=0.0.0.0
  #     --conf spark.executor.memory=512m
  #     --conf spark.executor.cores=1
  #   ports:
  #     - "10000:10000"   # HiveServer2 / Thrift JDBC
  #   networks:
  #     - vulcan


