version: v1alpha
type: vulcan
name: orders360
description: Daily sales analytics pipeline tracking order volumes, revenue trends, and customer activity across the e-commerce platform.
tags:
  - postgres
  - orders
  - analytics
spec:
  runAsUser: "shreyasikarwartmdcio"
  compute: pacific
  engine: postgres
  repo:
    url: https://bitbucket.org/rubik_/vulcan-examples
    syncFlags:
      - '--ref=main'
      - '--submodules=off'
    baseDir: vulcan-examples/postgres/easy/orders360-mini
    secret: sales:git-sync
  workflow:
    schedule:
      crons:
        - '0 0 * * *'
      endOn: '2027-01-01T00:00:00-00:00'
      timezone: 'US/Pacific'
      concurrencyPolicy: Forbid
    logLevel: INFO
    resource:
      request:
        cpu: "200m"
        memory: "512Mi"
      limit:
        cpu: "1000m"
        memory: "1Gi"
    plan:
      command:
        - vulcan
      arguments:
        - plan
        - --auto-apply
    run:
      command:
        - vulcan
      arguments:
        - run
  api:
    replicas: 1
    logLevel: INFO
    resource:
      request:
        cpu: "200m"
        memory: "512Mi"
      limit:
        cpu: "1000m"
        memory: "1Gi"
