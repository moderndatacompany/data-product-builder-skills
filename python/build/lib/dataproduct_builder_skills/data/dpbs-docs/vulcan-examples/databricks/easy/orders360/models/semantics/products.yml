kind: semantic
name: products
depends_on: sales.products
description: Product dimension (semantic layer)

dimensions:
  - product_id
  - product_name
  - category
  - subcategory
  - brand
  - color
  - size
  - price
  - rating
  - stock_quantity
  - discontinued

measures:
  - name: total_products
    type: count
    expression: "{products.product_id}"
    description: Total number of products

  - name: active_products
    type: count
    expression: "{products.product_id}"
    filters:
      - "{products.discontinued} = 0"
    description: Number of active (non-discontinued) products

segments:
  - name: high_rated_products
    expression: "{products.rating} >= 4.0"
    description: Products with rating >= 4.0
