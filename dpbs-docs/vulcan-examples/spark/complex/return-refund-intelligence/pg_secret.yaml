#name: postgres-lakehouse-secrets
name: ${LH_POSTGRES_SECRET_NAME}
version: v2alpha
type: secret
description: "PostgreSQL secrets for lakehouse metastore"
layer: user
secret:
  type: key-value
  data:
    username: ${LH_POSTGRES_USERNAME}
    password: ${LH_POSTGRES_PASSWORD}
