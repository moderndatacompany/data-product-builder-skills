# Converted from legacy: semantics/sales.yaml
kind: semantic
name: sales
depends_on: web_analytics_gold.SALES
ai_context:
  instructions: >
    Use this semantic model for questions about sales. Prefer its measures for
    aggregations and its dimensions for filtering, grouping, cohorting, and
    drill-downs.
  synonyms:
    - "sales"
  examples:
    - "Show sales trends over time."
    - "Break down sales by key dimensions."

measures:
- name: total_purchased_customers
  ai_context:
    instructions: >
      Use this measure when the question asks for total purchased customers. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "total purchased customers"
      - "total_purchased_customers"
      - "total"
      - "purchased"
    examples:
      - "What is total purchased customers by month?"
      - "Compare total purchased customers across key segments."
  type: number
  expression: COUNT(DISTINCT {sales.CUSTOMER_NO})
  description: total no of customers who had made purchase.
  tags:
  - customer
  - count
  - metric
  terms:
  - customer.total_purchased_customers
  - metric.customer_count
- name: total_invoices
  ai_context:
    instructions: >
      Use this measure when the question asks for total invoices. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "total invoices"
      - "total_invoices"
      - "total"
      - "invoices"
    examples:
      - "What is total invoices by month?"
      - "Compare total invoices across key segments."
  type: number
  expression: COUNT(DISTINCT CASE WHEN {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.INVOICE_NO} END)
  description: Total Invoices Generated.
  tags:
  - metric
  terms:
  - metric.total_invoices
- name: total_proof_invoices
  ai_context:
    instructions: >
      Use this measure when the question asks for total proof invoices. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "total proof invoices"
      - "total_proof_invoices"
      - "total"
      - "proof"
    examples:
      - "What is total proof invoices by month?"
      - "Compare total proof invoices across key segments."
  type: number
  expression: COUNT(DISTINCT CASE WHEN {sales.SOURCE} = 'Proof' THEN {sales.INVOICE_NO} ELSE NULL END)
  description: Total Proof Invoices Generated.
  tags:
  - metric
  terms:
  - metric.total_proof_invoices
- name: total_non_proof_invoices
  ai_context:
    instructions: >
      Use this measure when the question asks for total non proof invoices. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "total non proof invoices"
      - "total_non_proof_invoices"
      - "total"
      - "non"
    examples:
      - "What is total non proof invoices by month?"
      - "Compare total non proof invoices across key segments."
  type: number
  expression: COUNT(DISTINCT CASE WHEN {sales.SOURCE} = 'Non-proof' AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.INVOICE_NO} ELSE NULL END)
  description: Total Non Proof Invoices Generated.
  tags:
  - metric
  terms:
  - metric.total_non_proof_invoices
- name: total_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for total revenue. Respect its aggregation
      type and any configured filters before comparing or segmenting results.
    synonyms:
      - "total revenue"
      - "total_revenue"
      - "total"
      - "revenue"
    examples:
      - "What is total revenue by month?"
      - "Compare total revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.EXT_NET} ELSE 0 END)
  description: Calculates the total revenue generated.
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.total_revenue
  - finance.gross_sales
- name: proof_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for proof revenue. Respect its aggregation
      type and any configured filters before comparing or segmenting results.
    synonyms:
      - "proof revenue"
      - "proof_revenue"
      - "proof"
      - "revenue"
    examples:
      - "What is proof revenue by month?"
      - "Compare proof revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Proof' THEN {sales.EXT_NET} ELSE 0 END)
  description: Overall revenue generated by a business on 'Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.proof_revenue
  - finance.gross_sales
- name: non_proof_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for non proof revenue. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "non proof revenue"
      - "non_proof_revenue"
      - "non"
      - "proof"
    examples:
      - "What is non proof revenue by month?"
      - "Compare non proof revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Non-proof' AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.EXT_NET} ELSE 0 END)
  description: Overall revenue generated by a business on 'Non-Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.non_proof_revenue
  - finance.gross_sales
- name: proof_average_invoice_value
  ai_context:
    instructions: >
      Use this measure when the question asks for proof average invoice value. Respect
      its aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "proof average invoice value"
      - "proof_average_invoice_value"
      - "proof"
      - "average"
    examples:
      - "What is proof average invoice value by month?"
      - "Compare proof average invoice value across key segments."
  type: number
  expression: ROUND(SUM(CASE WHEN {sales.SOURCE} = 'Proof' THEN {sales.EXT_NET} ELSE 0 END) / NULLIF(COUNT(DISTINCT CASE WHEN {sales.SOURCE} = 'Proof' THEN {sales.INVOICE_NO} ELSE NULL END), 0), 2)
  description: Average invoice value of customer from proof source.
  tags:
  - metric
  - average
  - aggregate
  terms:
  - metric.proof_average_invoice_value
  - metric.aggregate
- name: r12_total_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for r12 total revenue. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "r12 total revenue"
      - "r12_total_revenue"
      - "r12"
      - "total"
    examples:
      - "What is r12 total revenue by month?"
      - "Compare r12 total revenue across key segments."
  type: number
  expression: SUM(CASE WHEN CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 12 MONTH AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.EXT_NET} ELSE 0 END)
  description: Calculates the recent 12 months of total revenue generated.
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.r12_total_revenue
  - finance.gross_sales
- name: r12_proof_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for r12 proof revenue. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "r12 proof revenue"
      - "r12_proof_revenue"
      - "r12"
      - "proof"
    examples:
      - "What is r12 proof revenue by month?"
      - "Compare r12 proof revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Proof' AND CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 12 MONTH THEN {sales.EXT_NET} ELSE 0 END)
  description: Recent 12 month revenue generated by a business on 'Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.r12_proof_revenue
  - finance.gross_sales
- name: r12_non_proof_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for r12 non proof revenue. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "r12 non proof revenue"
      - "r12_non_proof_revenue"
      - "r12"
      - "non"
    examples:
      - "What is r12 non proof revenue by month?"
      - "Compare r12 non proof revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Non-proof' AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} AND CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 12 MONTH THEN {sales.EXT_NET}
    ELSE 0 END)
  description: Recent 12 month revenue generated by a business on 'Non-Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.r12_non_proof_revenue
  - finance.gross_sales
- name: last30days_total_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for last30days total revenue. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "last30days total revenue"
      - "last30days_total_revenue"
      - "last30days"
      - "total"
    examples:
      - "What is last30days total revenue by month?"
      - "Compare last30days total revenue across key segments."
  type: number
  expression: SUM(CASE WHEN CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 30 DAY AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.EXT_NET} ELSE 0 END)
  description: Calculates the recent 30 days of total revenue generated.
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.last30days_total_revenue
  - finance.gross_sales
- name: last30days_proof_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for last30days proof revenue. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "last30days proof revenue"
      - "last30days_proof_revenue"
      - "last30days"
      - "proof"
    examples:
      - "What is last30days proof revenue by month?"
      - "Compare last30days proof revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Proof' AND CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 30 DAY THEN {sales.EXT_NET} ELSE 0 END)
  description: Recent 30 days revenue generated by a business on 'Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.last30days_proof_revenue
  - finance.gross_sales
- name: last30days_non_proof_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for last30days non proof revenue. Respect
      its aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "last30days non proof revenue"
      - "last30days_non_proof_revenue"
      - "last30days"
      - "non"
    examples:
      - "What is last30days non proof revenue by month?"
      - "Compare last30days non proof revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Non-proof' AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} AND CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 30 DAY THEN {sales.EXT_NET}
    ELSE 0 END)
  description: Recent 30 days revenue generated by a business on 'Non-Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.last30days_non_proof_revenue
  - finance.gross_sales
- name: prev30days_total_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for prev30days total revenue. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "prev30days total revenue"
      - "prev30days_total_revenue"
      - "prev30days"
      - "total"
    examples:
      - "What is prev30days total revenue by month?"
      - "Compare prev30days total revenue across key segments."
  type: number
  expression: SUM(CASE WHEN CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 60 DAY AND CAST({sales.POSTING_DATE} AS DATE) < CURRENT_DATE - INTERVAL 30 DAY AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST}
    THEN {sales.EXT_NET} ELSE 0 END)
  description: Calculates the previous 30 days total revenue generated.
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.prev30days_total_revenue
  - finance.gross_sales
- name: prev30days_proof_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for prev30days proof revenue. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "prev30days proof revenue"
      - "prev30days_proof_revenue"
      - "prev30days"
      - "proof"
    examples:
      - "What is prev30days proof revenue by month?"
      - "Compare prev30days proof revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Proof' AND CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 60 DAY AND CAST({sales.POSTING_DATE} AS DATE) < CURRENT_DATE - INTERVAL 30 DAY THEN
    {sales.EXT_NET} ELSE 0 END)
  description: Calculates the previous 30 days revenue generated by a business on 'Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.prev30days_proof_revenue
  - finance.gross_sales
- name: prev30days_non_proof_revenue
  ai_context:
    instructions: >
      Use this measure when the question asks for prev30days non proof revenue. Respect
      its aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "prev30days non proof revenue"
      - "prev30days_non_proof_revenue"
      - "prev30days"
      - "non"
    examples:
      - "What is prev30days non proof revenue by month?"
      - "Compare prev30days non proof revenue across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Non-proof' AND CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 60 DAY AND CAST({sales.POSTING_DATE} AS DATE) < CURRENT_DATE - INTERVAL 30 DAY
    AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.EXT_NET} ELSE 0 END)
  description: Calculates the previous 30 days revenue generated by a business on 'Non-Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.prev30days_non_proof_revenue
  - finance.gross_sales
- name: r12_total_revenue_until_prev_30days
  ai_context:
    instructions: >
      Use this measure when the question asks for r12 total revenue until prev 30days.
      Respect its aggregation type and any configured filters before comparing or
      segmenting results.
    synonyms:
      - "r12 total revenue until prev 30days"
      - "r12_total_revenue_until_prev_30days"
      - "r12"
      - "total"
    examples:
      - "What is r12 total revenue until prev 30days by month?"
      - "Compare r12 total revenue until prev 30days across key segments."
  type: number
  expression: SUM(CASE WHEN CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 13 MONTH AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} AND CAST({sales.POSTING_DATE} AS DATE) < CURRENT_DATE
    - INTERVAL 1 MONTH THEN {sales.EXT_NET} ELSE 0 END)
  description: Calculates the recent 12 months until previous month of total revenue generated.
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.r12_total_revenue_until_prev_30days
  - finance.gross_sales
- name: r12_proof_revenue_until_prev_30days
  ai_context:
    instructions: >
      Use this measure when the question asks for r12 proof revenue until prev 30days.
      Respect its aggregation type and any configured filters before comparing or
      segmenting results.
    synonyms:
      - "r12 proof revenue until prev 30days"
      - "r12_proof_revenue_until_prev_30days"
      - "r12"
      - "proof"
    examples:
      - "What is r12 proof revenue until prev 30days by month?"
      - "Compare r12 proof revenue until prev 30days across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Proof' AND CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 13 MONTH AND CAST({sales.POSTING_DATE} AS DATE) < CURRENT_DATE - INTERVAL 1 MONTH THEN
    {sales.EXT_NET} ELSE 0 END)
  description: Recent 12 month revenue until previous month generated by a business on 'Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.r12_proof_revenue_until_prev_30days
  - finance.gross_sales
- name: r12_non_proof_revenue_until_prev_30days
  ai_context:
    instructions: >
      Use this measure when the question asks for r12 non proof revenue until prev
      30days. Respect its aggregation type and any configured filters before comparing
      or segmenting results.
    synonyms:
      - "r12 non proof revenue until prev 30days"
      - "r12_non_proof_revenue_until_prev_30days"
      - "r12"
      - "non"
    examples:
      - "What is r12 non proof revenue until prev 30days by month?"
      - "Compare r12 non proof revenue until prev 30days across key segments."
  type: number
  expression: SUM(CASE WHEN {sales.SOURCE} = 'Non-proof' AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} AND CAST({sales.POSTING_DATE} AS DATE) >= CURRENT_DATE - INTERVAL 13 MONTH AND CAST({sales.POSTING_DATE}
    AS DATE) < CURRENT_DATE - INTERVAL 1 MONTH THEN {sales.EXT_NET} ELSE 0 END)
  description: Recent 12 month revenue until previous month generated by a business on 'Non-Proof'
  tags:
  - revenue
  - financial
  - metric
  terms:
  - revenue.r12_non_proof_revenue_until_prev_30days
  - finance.gross_sales
- name: wallet_share
  ai_context:
    instructions: >
      Use this measure when the question asks for wallet share. Respect its aggregation
      type and any configured filters before comparing or segmenting results.
    synonyms:
      - "wallet share"
      - "wallet_share"
      - "wallet"
      - "share"
    examples:
      - "What is wallet share by month?"
      - "Compare wallet share across key segments."
  type: number
  expression: ROUND(SUM(CASE WHEN {sales.SOURCE} = 'Proof' THEN {sales.EXT_NET} ELSE 0 END) / CAST(NULLIF(SUM(CASE WHEN {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.EXT_NET} ELSE
    0 END), 0) AS DOUBLE), 2)
  description: It retrieves the share of wallet in percentage.
  tags:
  - metric
  terms:
  - metric.wallet_share
- name: rev_per_account_non_proof
  ai_context:
    instructions: >
      Use this measure when the question asks for rev per account non proof. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "rev per account non proof"
      - "rev_per_account_non_proof"
      - "rev"
      - "per"
    examples:
      - "What is rev per account non proof by month?"
      - "Compare rev per account non proof across key segments."
  type: number
  expression: ROUND(SUM(CASE WHEN {sales.SOURCE} = 'Non-proof' AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.EXT_NET} ELSE 0 END) / NULLIF(COUNT(DISTINCT {sales.CUSTOMER_NO}),
    0), 2)
  description: Calculates avg of non-proof revenue per account
  tags:
  - metric
  terms:
  - metric.rev_per_account_non_proof
- name: rev_per_account_proof
  ai_context:
    instructions: >
      Use this measure when the question asks for rev per account proof. Respect its
      aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "rev per account proof"
      - "rev_per_account_proof"
      - "rev"
      - "per"
    examples:
      - "What is rev per account proof by month?"
      - "Compare rev per account proof across key segments."
  type: number
  expression: ROUND(SUM(CASE WHEN {sales.SOURCE} = 'Proof' THEN {sales.EXT_NET} ELSE 0 END) / NULLIF(COUNT(DISTINCT {sales.CUSTOMER_NO}), 0), 2)
  description: Calculates avg of proof revenue per account
  tags:
  - metric
  terms:
  - metric.rev_per_account_proof
- name: invoices_per_account_non_proof
  ai_context:
    instructions: >
      Use this measure when the question asks for invoices per account non proof.
      Respect its aggregation type and any configured filters before comparing or
      segmenting results.
    synonyms:
      - "invoices per account non proof"
      - "invoices_per_account_non_proof"
      - "invoices"
      - "per"
    examples:
      - "What is invoices per account non proof by month?"
      - "Compare invoices per account non proof across key segments."
  type: number
  expression: ROUND(COUNT(DISTINCT CASE WHEN {sales.SOURCE} = 'Non-proof' AND {sales.POSTING_DATE} >= {sales.PROOF_INVOICE_DATE_FIRST} THEN {sales.INVOICE_NO} ELSE NULL END) * 1.00 / NULLIF(COUNT(DISTINCT
    {sales.CUSTOMER_NO}), 0), 2)
  description: Calculates avg of non-proof invoices per account
  tags:
  - metric
  terms:
  - metric.invoices_per_account_non_proof
- name: invoices_per_account_proof
  ai_context:
    instructions: >
      Use this measure when the question asks for invoices per account proof. Respect
      its aggregation type and any configured filters before comparing or segmenting
      results.
    synonyms:
      - "invoices per account proof"
      - "invoices_per_account_proof"
      - "invoices"
      - "per"
    examples:
      - "What is invoices per account proof by month?"
      - "Compare invoices per account proof across key segments."
  type: number
  expression: ROUND(COUNT(DISTINCT CASE WHEN {sales.SOURCE} = 'Proof' THEN {sales.INVOICE_NO} ELSE NULL END) * 1.00 / NULLIF(COUNT(DISTINCT {sales.CUSTOMER_NO}), 0), 2)
  description: Calculates avg of proof invoices per account
  tags:
  - metric
  terms:
  - metric.invoices_per_account_proof
segments:
- name: is_proof
  ai_context:
    instructions: >
      Apply this segment when the question asks for the is proof subset. Combine it with
      measures to answer filtered business questions.
    synonyms:
      - "is proof"
      - "is_proof"
      - "proof"
    examples:
      - "Show revenue for is proof."
      - "How many records are in is proof?"
  expression: '{sales.SOURCE} = ''Proof'''
  description: Proof channel sales
  tags:
  - metric
  terms:
  - metric.is_proof
- name: is_non_proof
  ai_context:
    instructions: >
      Apply this segment when the question asks for the is non proof subset. Combine it
      with measures to answer filtered business questions.
    synonyms:
      - "is non proof"
      - "is_non_proof"
      - "non"
      - "proof"
    examples:
      - "Show revenue for is non proof."
      - "How many records are in is non proof?"
  expression: '{sales.SOURCE} = ''Non-proof'''
  description: Non-Proof channel sales
  tags:
  - metric
  terms:
  - metric.is_non_proof
